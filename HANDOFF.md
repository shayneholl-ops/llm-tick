# llm-tick handoff (2026-09-19)

Portable state for picking up this project in a fresh session. Read this first, then `README.md` (Hardware truth section) and `src/board.h` comments.

## What this is
A Waveshare **ESP32-S3-LCD-1.47B** (ST7789 172×320, 8 MB PSRAM, 16 MB flash) turned into a live **LLM-usage display**: polls the user's `server.py` (port 8266, mDNS `GMKK12`) every 60 s, shows session/weekly usage, auto-switches to a **weather standby** scene (WeatherAPI.com, Vancouver, background follows the actual day/night + condition) after 90 s of no data change. The 8 genart effect scenes were removed 2026-09-21 at the user's request and replaced by one **cyberpunk** ambient scene (5 procedural sub-scenes; item 7 below) — the board cycles usage ↔ weather ↔ cyberpunk with a `PRESS` line over USB-serial. **Button truth (2026-09-21, user-corrected):** the unit DOES have a RESET and a BOOT button (the earlier "no button" claim was wrong): a RESET press does a full reboot through the USB-UART chip (ROM `rst:0x15 (USB_UART_CHIP_RESET)`) and just returns the board to the usage scene; no BOOT press ever produced a scene change (90 s of serial watching) — the `checkButton()` GPIO0 poll is inert, so `PRESS` remains the only scene control.

## Locations
| Thing | Path / value |
|---|---|
| Canonical repo (master @ `a1a089f`, pushed 2026-09-21) | `C:\Users\hxp-n\llm-tick-repo\` |
| Debug toolkit (diag project + tools + evidence) | `C:\Users\hxp-n\llm-tick-diag\` (wxprobe, sweep, bandwatch, matrix, beat, bandmap, crashloop, Waveshare demo, capture clips, logs) |
| Stale pre-push sandbox copy (no .git, reference only) | `C:\Users\hxp-n\llm-tick\` (contains `ref-genart\`) |
| Sample feed data | `C:\Users\hxp-n\llm-tick-test\` |
| Board port | **COM4** (USB-Serial/JTAG), flash: `pio run -d C:\Users\hxp-n\llm-tick-repo -t upload --upload-port COM4` (~30 s) |
| pio / python | `C:\Users\hxp-n\AppData\Local\hermes\hermes-agent\venv\Scripts\{pio.exe,python.exe}` |
| Camera (the only "eye") | OctoStream RTSP `rtsp://192.168.1.86:554/stream`, 60 fps source; grab: `ffmpeg -rtsp_transport tcp - <url> -frames:v 1 -update 1 -y out.jpg`. **Service is flaky** (OctoPrint on the Pi; if 554 is refused, user restarts it on the Pi). |
| Secrets | `src/secrets.h` (gitignored): WiFi `Coromandel`, weather key filled, server 192.168.1.69:8266. **Never echo or commit.** Pushes use `gh` credentials; if a push fails on auth, re-run `gh auth setup-git`. |

## State: flicker fixed; crash fixed; dynamic Ferrari standby; **backlight white-balance (committed `d507d07`)**
- `0a6abb0` — three **per-unit hardware facts** (backlight GPIO48; glass centered `offset_x 34`; glass mounted **180°** → `offset_rotation 2`).
- `4de0fbb` — weather key live; serial `PRESS` line emulates BOOT (hands-free scene testing) — the **only** scene control (RESET only reboots; BOOT is not a scene input on this build).
- `c621bed`+`e79029a`+`d248a6e` — 2026-09-18 top-band investigation, *then* closed as optical — **now superseded** (below).
- **2026-09-19 (this session)**:
  1. **Flicker root cause (proven in the dark with the camera at 4 fps):** the ST7789's gate-array **end — the last ~60 RAM rows — meanders in luminance** (stochastic ±5 % of local level, broadband + ~25 s drift; NOT a periodic beat — 60 s autocorrelation is flat, peak 0.23). It **requires backlight on** (vanishes at BL 0), scales ~linearly with backlight duty (5.2/4.5/4.4 % at 100/50/25 % duty), is **content-independent**, is angle-independent, and **follows RAM-row identity, not physical glass**: with the flip removed the band moves to the opposite edge (bandBot Δ2.26 / bandTop Δ0.09 unflipped ↔ bandTop Δ1.65–2.8 flipped). This unit's 180° mount puts RAM-row-319-at-edge at the user's **top** edge → the perceived "flickering band". The 25/60 fps cameras of 2026-09-18 undersampled it (the meander's energy is at ~0.04–2 Hz; 4 fps resolves it, 25 fps partially aliases it away). The reference machine (no flip) parks the same meander at the bottom edge, unnoticed.
  2. **Flicker fix (shipped):** keep display **rows 0–59 pure background** in both UI scenes (ui.cpp layout shift: title y 8→64, divider 38→94, bars 52/128/204→104/176/248, tokens info row 42→96, status h-24→h-16, weather big number 60→72). A ±5 % meander on a uniform field has no edges to modulate → imperceptible. (The earlier 1-row mirror guard — `memcpy` row 1→row 0 — does NOT fix it; the band is ~60 rows, and the meander modulates the row's own luminance regardless of content. Guard kept anyway as a row-319 nicety; it is harmless.)
  3. **Dynamic weather background (user request):** the standby scene's background now follows the real conditions — WeatherAPI `current.is_day` × condition code — and **eases ~1 s** between targets (per-frame 1/10 lerp in `ui.cpp`). v1 was a bright day/night palette (sky-blue etc.) — **user rejected ("the background is bad")** → v2 per `DESIGN-ferrari.md` (Ferrari brand system, in-repo): near-black `#181818` canvas with only a subtle per-condition tint (all 8 targets are dark grays/warmth/cool), white ink, gray body, muted captions (uppercase), one scarce **Rosso Corsa accent on the clock** (the "race position" role). Also fixed the clock, which showed UTC (`gmtime`) despite the board's `PST8PDT` TZ — now `localtime`. On-screen hint now says `PRESS: CYCLE SCENES`.
  4. **Backlight white-balance (user-approved "software white-balance" fix):** the user's "pink background" photos were root-caused to the backlight itself — **this unit's backlight is a single cool-cast (blue/violet-rich) LED, not white.** Proven by the `BLX` polarity matrix (new serial command; pins [48,46,47] in 8 states): **GPIO48 is the only live channel — 46/47 are dead in both polarities** (the wiki's 3-channel scheme does not exist on this build). All-black content (`PAT 4`) still glows periwinkle through the polarizer; camera AWB renders that cast **pink in photos** (the eye sees a dim violet). Fix shipped in `d507d07`: **`wb565()` in `tick.h` pre-shifts every UI/effect color toward green (round 2: R×16/16, G×14/16, B×10/16)** so neutral content emits neutral through the cast — `uiWbInit()` rewrites the UI palette in `setup()`, `buildTables()` applies it to effect palettes, `wxBgTarget` too. **Calibration method (use this, not raw dark-region photo hues):** `WB 7` (new serial command) shows **raw white on one half (locks the camera's AWB at full correction) + the corrected dark canvas on the other** — the dark half's measured hue *is* the true residual cast. Results: no-gain dark canvas U≈140/V≈123 (violet); round 1 (G×15/16, B×12/16) left B/R≈1.19; **round 2 (G×14/16, B×10/16): dark canvas B/R≈1.05, U≈130/V≈125 — neutral to within camera noise; white half still neutral (U=128±3)**. Do not push gains further — the camera's AWB clamps at the white point. **Camera caveat (bites every session): in a frame where the screen is entirely dim, the camera's scene-level AWB decides "scene is blue" and pushes red — the dim background reads PINK in the photo even though it is physically neutral. The split field is the only controlled dark-content test.** Raw `WB 1..6` fields (white/gray/primaries) are deliberately uncorrected — they are the calibration tools.
  5. **Crash found + mitigated (pre-existing, not from this session's code):** under a bursty-load stress loop the board crash-looped ~every 24–48 s: `wifi:ebuf_free: invalid type` + `assert block_locate_free` / `multi_heap_free (head != NULL)` — the wifi driver's **dynamic RX ebuf pool (32 × ~1.7 KB, malloc'd per frame burst)** corrupting TLSF heap metadata (detected in `wDev_ProcessRxSucData → wifi_malloc`). Proven by 15-min loops (`crashloop.py <seconds> <pressEvery>`): 6-s PRESS hammer = RED on **every** build (full, −blTask, −guard, **pristine d248a6e control** — my code exonerated); **radio disabled = 0 crashes/15 min GREEN** → the trigger is WiFi RX load (the Pi streams 1080p RTSP all night; today's AP is busier than 2026-09-18's). Normal 60-s polling = **0 crashes/15 min GREEN** (why the board never crashed in real use).
  - **Shipped mitigation:** `tickLogic` now gates the on-switch refreshes to **once per 30 s** (usage + weather), so even a mashed `PRESS` line can't burst the driver past ~2 fetches/min. Verified: the same 6-s hammer that crash-looped every build before now runs clean. NOTE: a `sdkconfig.defaults` with fully-static RX buffers was tried first — **the ARDUINO build ignores it** (prebuilt driver SDK; only the pure-ESP-IDF build path honors sdkconfig), so the file was dropped; the driver's pool size is not tunable per-project in this framework. If crashes ever return, the cures are: power-cycle the router (AP-side), or rebuild the app on the espidf framework where `STATIC_RX_BUFFER_NUM=32`/`DYNAMIC_RX_BUFFER_NUM=0` actually compile in.
  - Bisect table (15-min loops, 2026-09-19): full-diag+layout @6-s-hammer **crash ~24 s/boot**; −blTask **~24 s/boot**; −guard **~45 s/boot**; d248a6e control **~45 s/boot**; d248a6e+NO_WIFI **0 crashes**; full build @60-s-normal **0 crashes**; final build @6-s-hammer **see commit (expected green)**.
   6. **Genart effects removed (2026-09-21, user request — "I don't need purzbeats/esp32-147b-genart"):** scenes 2..9 and `effects.h/.cpp` (8 effects + 12 palettes + `buildTables()`/`effectsSeed()`) are deleted; `sceneCount()` is 2 (usage ↔ weather — one `PRESS` toggles, boot lands on usage), `showLed` keeps two colors (usage blue, standby green — the old WS2812 effect-color quirk note is moot). Kept: the dual-core pipeline, `board.h`, and all diagnostic serial commands (`PAT`/`WB`/`BL`/`BLX`/`ST`). Same session: docs corrected to the button truth above; on the .69 PC, 4 stale `server.py` copies were killed and one clean instance restarted (LAN-bound, `LOG_PATH` = the harness's `~/.hindsight/coding-agents-logs/usage.jsonl` — that log is the board's real data source; the .94 box hosts the llama-server inference backend, not the accounting). The board's 0 % readings after 00:16 were the 5 h/7 d windows rolling over at midnight — a healthy feed, not a failure.
   7. **Cyberpunk ambient scene added (2026-09-21, user request — "add Oxpr0x/Waveshare-ESP32-S3-Cyberpunk-Display no sd"):** scene **2** is one auto-cycling ambient scene holding five procedural sub-scenes — matrix rain (ASCII + half-width katakana), procedural night skyline (blinking windows, aircraft), glitch storm, hex-dump scroll, neural-net pulses — ~9–14 s each with a 3-frame black hold between (`cyberpunk.cpp`, `cyberInit()`/`cyberFrame()`). **Written fresh for this repo, not copied:** the Oxpr0x repo (a landscape 320×172 TFT_eSPI sketch) declares **no license**, so only its *concepts* were used; the five effects were re-implemented for this pipeline and re-laid-out for our **portrait 172×320** glass (21 rain columns, buildings 60–170 px tall with the horizon at y 148, hex dump at 3 bytes/line to fit 21 columns of the 8×8 font). Only its public-domain glyph DATA is vendored: `src/font8x8.h` (ASCII from font8x8_basic — Daniel Hepper/Marcel Sondaar/IBM VGA; katakana from Noto Sans CJK JP, OFL) with attribution in the file header. No SD card, no TFT_eSPI, no new libraries: the scene draws straight into llm-tick's framebuffer through a small shim (fillRect/px/line/circle/glyph) with colours run through `wb565()` + byte-swap like the UI palette. Sub-scene changes log `[cyber] sub-scene N (name)`. Verified on-unit: boots, `PRESS`×2 → `scene=2 (cyberpunk)`, sub-scenes advance (matrix→city→glitch→hexdump), ~13.6 fps, no asserts. Design notes: the per-frame interlace fade of the original would flicker here (the two framebuffers alternate and would hold different fade levels), so the transition is a black hold + a collapsing scan line; scene 2 is manual — the idle/activity rules in `data.cpp` still only touch scenes 0/1, so an active feed does **not** pull the board off cyberpunk.
   8. **Usage feed was empty — root-caused and wired to real counters (2026-09-21):** the user asked "why no data" on the usage page. The board and `server.py` were healthy — the *feed* was empty. Two findings: (a) the file the server was reading, `~/.hindsight/coding-agents-logs/usage.jsonl`, is **Hindsight call telemetry** (`ts/harness/session/bank/turn/calls/credited` — no model, no tokens), so every row parsed as 0 tokens with model `"?"`; (b) **no genuine token log exists anywhere on this machine** — the only file with token fields is the toy fixture `C:\Users\hxp-n\llm-tick-test\usage-test.jsonl` (2 fake models, ts 2026-09-18), which is what the earlier `server.py` instances were reading (hence "2 models" but 0 %: its rows sit outside the live 5 h/24 h windows). So the usage page never had real data. **Fix shipped:** the `.94` llama-server (systemd *user* unit `llama-server.service`, plus `llama-watchdog.py` which only acts on wedged generations) now runs with `--metrics` via a drop-in at `~/.config/systemd/user/llama-server.service.d/metrics.conf` (clears `ExecStart` then re-adds it with `--metrics`; survives restarts). It exposes cumulative `llamacpp:prompt_tokens_total` / `llamacpp:tokens_predicted_total`. New **`llama_usage_poller.py`** (repo root) samples `http://192.168.1.94:12345/metrics` every 30 s, diffs the counters (a decrease = new baseline after a restart; no negative rows) and appends `{"ts","model","input_tokens","output_tokens"}` rows to `C:\Users\hxp-n\llm-tick-usage\usage.jsonl`. `server.py` now runs with `LOG_PATH` = that file (LAN-bound, one instance; the earlier stray copies were killed). Verified end-to-end with a real 12-in/40-out completion: poller row written, `server.py` → `models:[{name:"Qwen3-27B",tok:"52"}]`, board serial → `weekly=100% models=1 tok=52`. **Caveats:** usage while the poller is stopped is lost (start it at logon — not yet a scheduled task); the bars are `session = 5 h tokens / 2 M` and `weekly = top model's share of 24 h` (pegged 100 % with one model) — both one-line changes in `server.py` if the user wants real budgets.
   9. **Weather page: no LVGL — and a procedural animated icon shipped (2026-09-21, user request):** the user asked "what LVGL did it use? I don't see any icon animation and background". Answer: **llm-tick uses no LVGL at all** — the stack is LovyanGFX (`lib_deps`: LovyanGFX, ArduinoJson, NeoPixel) with hand-rolled drawing in `ui.cpp`, and the weather scene was text on a flat eased tint, so there were never any icons or animation. The expectation comes from the credit `weather_api.*` → [icefox0801/ESP32-S3-LCD-1.47-Tiny-Board](https://github.com/icefox0801/ESP32-S3-LCD-1.47-Tiny-Board), which **is** an LVGL 9.3 UI with 64 × 64×64 PNG icons loaded from SPIFFS via LODEPNG; only its WeatherAPI client was merged, never its UI. (Even upstream has no icon *animation* — "animation library integration" is an unchecked future item there.) **Shipped instead, procedurally:** `ui.cpp` draws the condition icon from primitives — families sun / moon / partly (day, night) / cloud / rain / snow / storm / fog, mapped from WeatherAPI codes — animated from `millis()` phases: sun rays breathe, clouds drift sideways, rain streaks and snow flakes fall, the moon's halo pulses, a storm flashes **locally inside the icon box only** (never full-screen — the panel already has a flicker history), fog bands slide. Monochrome per `DESIGN-ferrari.md` (ink shape / body puffs / muted detail; Rosso Corsa stays on the clock alone). Placement: the free block right of the temperature, auto-shrinking 48→40→32 px if the reading is wide (e.g. "-10"); the icon lives below y 62 so display rows 0-59 stay pure background (top-band meander guard). No assets, no new libraries. Verified on-unit: boots, weather scene runs at 15.8 fps, render 7→8 ms, no asserts. **Camera-verified 2026-09-21 (RTSP back up; the session's model changed to one with vision):** RTSP frames show the icon on the glass — the then-current "Smoky haze" (code 1130 → plain cloud family) rendered as a drifting cloud right of the big 14°, and two crops 6 s apart show the drift phase moved, i.e. the animation runs; the warm haze background tint, red clock, and `PRESS: CYCLE SCENES` footer all read correctly. The API later flipped to "Partly Cloudy" (1003); the board follows it on its 30-min weather cadence. The user's eyes remain the final visual judge. Note: mid-capture the board reset once through the USB-UART chip (`rst:0x15`, no crash backtrace, recovered to a healthy 79-push pipeline) — suspected USB/DTR edge on the front-panel port, not a firmware fault; watch for repeats.
    10. **Weather background: per-condition x day/night two-stop gradient (2026-09-21, user request — "build on top of it" after checking the icefox0801 repo):** the upstream check confirmed there is nothing to port — their weather screen is a flat white background with a static dark-purple card (0x29006b) + static teal AQI card (0x004d40), no gradients, no animated backgrounds, no background assets (the 64 icon PNGs are the only media), and no LICENSE file (concepts only). Built natively on our LovyanGFX scene instead: `renderWeather()` fills rows 0-59 with the tuned per-condition x day/night **top** target (the meander-guard band stays one uniform color) and writes rows 60-319 as a per-row vertical gradient descending to a static bottom stop at ~55 % of the top luminance (`mix565`/`scale565` in `ui.cpp`, WB-safe); both stops ease 1/10 per frame only while a condition change is in flight (~1 s); all text clips via `bgAt(y)` to the local row color; polarity is judged at the gradient midpoint. **Two on-glass bugs found and fixed during bring-up:** (a) a `mix565` operator-precedence bug — `ar + (br-ar)*f >> 10` parses as `(ar + (br-ar)*f) >> 10` (`+` binds tighter than `>>` in C) → channels wrapped into bright garbage = white screen; (b) a first per-frame ~40 s luminance "breath" on the bottom stop made the panel rolling per-scan refresh band across the whole lower field (horizontal stripes in a camera capture) — the field is now static per condition; the only per-frame motion is the animated icon (same constraint family as the top-band meander). Camera-verification status: the fixed build is live on the board (parked on the weather scene, `ST` confirms scene=1), but RTSP died mid-verification (554 refused — OctoPrint on the Pi is down until user restarts it); final judge is the user at the glass. **Crash data point (2026-09-21):** with `server.py` stopped (the board was retrying the dead port on its 10 s fail-fetch cadence) it hit `assert failed: block_trim_free heap_tlsf.c:371` (TLSF) and rebooted; clean recovery. Same family as the WiFi RX ebuf corruption (item 5) — the 10 s fail-poll burst is a new stress variant to watch. The server is back up (LAN-bound instance, `LOG_PATH=C:\Users\hxp-n\llm-tick-usage\usage.jsonl`, `BIND_HOST=0.0.0.0`) and the poller runs again.
   
- Board is running live in the user's PC (USB end up); WS2812 ring shows scene color (working — don't "fix" without an on-unit test).
- Serial commands live in the build (`PRESS` = the scene cycler / `LED 0|1` / `BL 0|25|50|100` / `BLX 0..6` / `PAT 0..6` / `WB 0..7` / `ST`) — harmless at runtime (BL 100 = DC, PAT 0 = normal, WB 0 = normal), kept on purpose; 200-Hz-class backlight is a 10-ms-tick 5-substep task (`blTask`), **not** delayMicroseconds (that froze the board) — do not reintroduce spins.

## Camera measurement facts (use these, not the 25/60 fps recipes, for panel light)
- 1080×1920 RTSP regions: bandTop (330,515,230,55) = display rows ~12–37; bandMid (320,840,280,60); bandBot (340,1130,240,50); caseCtrl (100,100,400,80) = ambient control; pwrBtn (150,350,130,110).
- **Measure the meander at 4 fps** (matrix.py, 48 frames = 12 s): a real meander shows Δ≈1.7–2.8 in the band vs ≤0.1 elsewhere; a clean region reads ≤0.1 at 4 fps. In the dark, compare **deltas and within-run ratios, never absolute Y** (camera auto-exposure drifts run-to-run; caseCtrl is the control). pwrBtn shows a separate ~2 % case power-LED breathing — ignore it.
- 10 fps × 60 s (`beat.py`) resolves the meander's spectrum; 10 fps is the floor for seeing ~1–2 Hz structure.

## Open (user-side, 30 s each)
0. **Eyes on the background (2026-09-19, after `d507d07`):** ask the user to look at the dark standby background in person — the camera's photos still show pink (scene-AWB artifact in the dark; see item 4 above), so the user's eye is the judge. If the dim glow still reads violet to them, the lever is the two gain constants in `wb565()` (tick.h) — strengthen B (10→9/16) and re-verify with `WB 7`.
1. **Band shroud (user's choice, 2026-09-21):** the user still sees the meander breathing on the weather background (camera: 4.36 Y peak-to-peak in the band vs 0.25 desk control at 4 fps — not a power dip, not data) and picked a **black shroud over the top ~7 mm of the glass** (the band spans ~60 of 320 rows ≈ 6–7 mm; content starts ~7 mm below the top edge) over the 180°-rotation alternative — user-side, no config change, no reflash. Once it's on, verify with a 4 fps camera capture (band region should read covered/dark, no wobble).
2. **The AP:** the stress-crash was triggered by bursty WiFi RX load (the Pi streams 1080p RTSP to the session all night; today's AP is busier than on 2026-09-18). The 30-s refresh gate (shipped) stops scene-mashing from bursting the driver, and normal 60-s polling was already clean — but if any wifi oddity ever returns, power-cycling the router is the complementary cure.

## Gotchas
- **Per-unit variance is real**: another 1.47-family machine (genart reference, COM3) runs BL 46 / `@34` / 320×172 fine. Trust on-unit camera + serial evidence over any schematic.
- ST7789 has **no readback**; `lcd.init()` returns true even with a wrong pinout — hardware evidence wins.
- If the panel goes dark again: suspect **USB power delivery** (backlight draw) first.
- `LGFX_Sprite` has no plain `fill(color)` — full fill is `fillRect(0,0,w,h,color)`.
- Standby needs 90 s of **no data change**; a live feed that moves will keep the board on the usage scene.
- Camera moves between runs — re-derive framing each time; the board sits in the upper camera area, USB end up, and **the board itself gets tilted/moved between captures** (happened repeatedly 2026-09-19 — never reuse a crop from an earlier frame without looking).
- **Camera hue recipe**: crop the region → `format=rgb24 -f rawvideo` → average bytes; hue via `scale=64:64,format=yuv420p` raw (64×64 avoids rawvideo line-padding) and average the U/V planes — neutral is U=V=128, violet is U>128. pwsh gotchas hit on this path: the format operator is `-f` (not C-style printf), and a function named `Measure` collides with the `Measure-Object` cmdlet — name helpers `Measure-Crop` etc.
- `sendcmd.ps1` args: PowerShell quoting eats arrays — one simple quoted string per command (`-Cmds "PRESS"`), or the board receives one mashed line and ignores it.
- **crashloop.py owns COM4** — don't run sendcmd.ps1 while a loop is running (exclusive port).
